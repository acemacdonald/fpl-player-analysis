# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# --- Data (raw snapshots and the DuckDB file are regenerated by `fpl refresh`) ---
data/raw/*
!data/raw/.gitkeep
data/*.duckdb
data/*.duckdb.wal

# --- Generated reports are committed on purpose (they are the weekly decision log) ---
# reports/ is tracked. Remove the line below if you'd rather keep them local.
# reports/*.md

# --- Cowork workspace memory (personal, never pushed) ---
MEMORY.md

# --- OS / editor ---
.DS_Store
.vscode/
*.log
